{
  "license": "UNLICENSED",
  "description": "a map interface to show vaccinated-friendly enterprises in Latvia -  for the portal vund.lv ",
  "source": "src/index.html",
  "main": "public/index.html",
  "scripts": {
    "watch": "cross-env-shell NODE_ENV=development \"parcel serve src/index.html --host localhost\"",
    "build": "cross-env-shell NODE_ENV=production \"rimraf public /Q && parcel build src/index.html --public-url ./ \""
  },
  "dependencies": {
    "@types/leaflet": "^1.7.4",
    "@vercel/node": "^1.11.1",
    "cross-env": "^7.0.3",
    "http-status-codes": "^2.1.4",
    "latinize": "^0.5.0",
    "leaflet": "^1.7.1",
    "parcel": "^2.0.0-beta.2"
  },
  "devDependencies": {
    "parcel-plugin-obfuscate": "^2.1.1",
    "rimraf": "^3.0.2"
  }
}
